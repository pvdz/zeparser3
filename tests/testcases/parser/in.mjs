import {$ASI, $IDENT, $PUNCTUATOR} from '../../../src/zetokenizer';

export default (describe, test) => describe('in operator', _ => {});

// TODO: various cases of checking that `in` parses properly (LF_NO_IN), including nested stuff and resetting of flag
// TODO: various for-in edge cases
// TODO: `in` whitespace edge cases
